1. show all activities from all specified repo paths in config file
2. cmdline neofetch-like interface & stats
3. anki/github-like calendar heatmap
    3.1 configurable unicode heatmap, like what decimal rank uses what symbol, (for example ascii art only or coloured unicode)
4. repository tierlist
5. configure authors to consider by email (in config)
